from ..model import Module, Task

LAB = "/root/lab"

m01 = Module(
    id="m01", title="Навигация по файловой системе", level="новичок",
    tasks=[
        Task(
            id="m01-01", title="Где я нахожусь",
            brief="""Файловая система Linux — одно дерево, начинающееся с корня `/`.
В отличие от Windows, здесь нет «дисков» C:/ D:/ — всё монтируется внутри этого дерева.

Каждый процесс имеет «текущий каталог» — точку, относительно которой
разрешаются пути без `/` в начале. При логине это обычно домашний каталог.

Ключевые команды:
  pwd          # print working directory — где я сейчас
  cd /path     # перейти в каталог по абсолютному пути
  cd           # без аргумента — вернуться в домашний каталог (~)

Перенаправление `>` записывает вывод команды в файл:
  echo "текст" > файл.txt   # создаёт/перезаписывает
  pwd > файл.txt             # записывает текущий путь в файл

Задание: убедись, что ты находишься в `/root/lab`, и запиши текущий путь в файл `where.txt`.""",
            setup=f"mkdir -p {LAB}; rm -f {LAB}/where.txt",
            check=f"grep -qx '/root/lab' {LAB}/where.txt",
            hints=["Сначала `cd /root/lab`", "Потом `pwd > where.txt`"],
            solution="cd /root/lab && pwd > where.txt",
        ),
        Task(
            id="m01-02", title="Абсолютные и относительные пути",
            brief="""Путь — это адрес файла или каталога в дереве ФС.

Абсолютный путь: начинается с `/`, работает из любого места:
  /etc/passwd       # всегда один и тот же файл
  /root/lab/notes   # полный путь

Относительный путь: отсчитывается от текущего каталога:
  documents/file.txt    # подкаталог текущего
  ../sibling/file.txt   # на уровень выше, потом в sibling

Специальные обозначения:
  .       текущий каталог
  ..      родительский каталог
  ~       домашний каталог текущего пользователя (/root для root)
  -       предыдущий каталог (cd - возвращает назад как «кнопка назад»)

  mkdir -p a/b/c   # создаёт всю цепочку каталогов сразу

Задание: сходи в `/etc`, оттуда — обратно одной командой, а затем создай в `~/lab` дерево каталогов `a/b/c` одной командой.""",
            check=f"test -d {LAB}/a/b/c",
            check_history=r"cd\s+/etc",
            hints=["`cd /etc`, затем `cd -`", "`mkdir -p a/b/c` — ключ `-p` создаёт цепочку каталогов"],
            solution="cd /etc; cd -; mkdir -p ~/lab/a/b/c",
        ),
        Task(
            id="m01-03", title="ls и скрытые файлы",
            brief="""`ls` — самая используемая команда. Выводит содержимое каталога.

Важные ключи:
  -l    длинный формат: права, владелец, размер, дата, имя
  -a    показать скрытые файлы (имена начинаются с точки: .bashrc, .ssh)
  -h    «человекочитаемые» размеры (1K, 2.3M вместо байт)
  -t    сортировка по времени изменения (новые первыми)
  -r    обратный порядок сортировки
  -S    сортировка по размеру

Скрытые файлы — это просто файлы с именем, начинающимся на `.`.
Никакой особой защиты — они просто не отображаются при `ls` без флага `-a`.
Используются для хранения настроек (.bashrc, .ssh/config, .gitignore).

  touch .secret      # создать пустой скрытый файл
  ls -la             # подробный список включая скрытые

Задание: создай скрытый файл `.secret` в `~/lab` и сохрани подробный листинг каталога (со скрытыми файлами) в `listing.txt`.""",
            setup=f"rm -f {LAB}/.secret {LAB}/listing.txt",
            check=f"test -f {LAB}/.secret && grep -q '\\.secret' {LAB}/listing.txt",
            hints=["`touch .secret` создаст пустой файл", "`ls -la > listing.txt`"],
            solution="touch ~/lab/.secret && ls -la ~/lab > ~/lab/listing.txt",
        ),
        Task(
            id="m01-04", title="Смотрим на систему",
            brief="""Стандарт FHS (Filesystem Hierarchy Standard) определяет назначение каталогов.
Знать структуру — значит сразу понимать, куда смотреть при проблемах.

  /           корень, начало всего дерева
  /etc        конфигурационные файлы системы и служб
  /var        изменяемые данные: логи (/var/log), базы данных, очереди
  /usr        установленные программы (/usr/bin, /usr/lib, /usr/share)
  /bin        основные команды (в современных системах → /usr/bin)
  /home       домашние каталоги пользователей (/home/alice, /home/bob)
  /root       домашний каталог суперпользователя root
  /tmp        временные файлы (очищается при перезагрузке)
  /proc       псевдо-ФС ядра: /proc/cpuinfo, /proc/meminfo, /proc/1/
  /dev        файлы устройств: /dev/sda, /dev/null, /dev/random
  /sys        другая псевдо-ФС: параметры ядра и оборудования
  /opt        опциональный soft, установленный вручную

Задание: сохрани подробный список содержимого `/etc` в файл `~/lab/etc-list.txt`.""",
            check=f"grep -q 'hosts' {LAB}/etc-list.txt && grep -q 'passwd' {LAB}/etc-list.txt",
            hints=["`ls -l /etc > ~/lab/etc-list.txt`"],
            solution="ls -l /etc > ~/lab/etc-list.txt",
        ),
        Task(
            id="m01-05", title="Дерево каталогов",
            brief="""`tree` рисует структуру каталогов в виде дерева — удобно для понимания
организации проекта или быстрой ориентации на незнакомом сервере.

  tree /etc             # всё дерево от /etc (может быть огромным)
  tree -L 1 /etc        # только один уровень вглубь
  tree -L 2 /usr        # два уровня
  tree -d /var          # только каталоги, без файлов
  tree -a /root         # включая скрытые файлы

Если `tree` не установлен:
  find /etc -maxdepth 1 -ls    # аналог через find

`tree` отлично показывает, «что вообще здесь есть», когда заходишь
на незнакомый сервер или изучаешь структуру нового проекта.

Задание: сохрани дерево каталога `/etc` глубиной 1 уровень в файл `~/lab/tree.txt`.""",
            check=f"grep -q 'directories' {LAB}/tree.txt && grep -q 'systemd' {LAB}/tree.txt",
            hints=["`tree -L 1 /etc > ~/lab/tree.txt`"],
            solution="tree -L 1 /etc > ~/lab/tree.txt",
        ),
        Task(
            id="m01-06", title="Справка: man и --help",
            brief="""Умение читать документацию — главный навык. Гуглить — медленно;
справка встроена прямо в систему и всегда актуальна.

`man команда` — полное руководство (manual page):
  Навигация: стрелки / PgUp / PgDn, пробел — следующая страница
  Поиск: /слово Enter, затем n — следующее совпадение, N — предыдущее
  Выход: q

`команда --help` — краткая справка, выводится в терминал сразу.
`man -k ключевое_слово` — найти man-страницы по теме (например: man -k network)
`whatis команда` — однострочное описание команды

Страницы man разбиты на разделы:
  1   пользовательские команды (ls, grep, find)
  5   форматы конфигурационных файлов (fstab, passwd)
  8   команды администратора (mount, useradd)

man 5 passwd — читать о формате файла /etc/passwd (не команды passwd)

Задание: посмотри `man ls`, а затем сохрани короткую справку `ls` в `~/lab/lshelp.txt`.""",
            check=f"grep -q 'almost-all' {LAB}/lshelp.txt",
            check_history=r"\bman\b",
            hints=["`man ls`, выход — клавиша q", "`ls --help > ~/lab/lshelp.txt`"],
            solution="man ls; ls --help > ~/lab/lshelp.txt",
        ),
    ],
)

m02 = Module(
    id="m02", title="Операции с файлами и каталогами", level="новичок",
    tasks=[
        Task(
            id="m02-01", title="Создание файлов пачкой",
            brief="""`touch` делает две вещи:
1. Создаёт пустой файл, если его не существует.
2. Обновляет время последнего изменения, если файл есть.

Второй случай полезен при автоматических скриптах, которые ориентируются на mtime.

Раскрытие фигурных скобок (brace expansion) — мощная возможность bash.
Shell сам строит нужные имена ДО запуска команды:

  touch file{1,2,3}.txt        # → file1.txt file2.txt file3.txt
  touch log_{a,b,c}.log        # → log_a.log log_b.log log_c.log
  mkdir -p dir/{src,bin,docs}  # создаёт сразу три подкаталога

Можно использовать диапазоны:
  touch report{01..10}.txt     # report01.txt ... report10.txt

Работает с любой командой, не только touch и mkdir.

Задание: одной командой создай в `~/lab` файлы `one.txt`, `two.txt`, `three.txt`.""",
            setup=f"cd {LAB} && rm -f one.txt two.txt three.txt",
            check=f"test -f {LAB}/one.txt && test -f {LAB}/two.txt && test -f {LAB}/three.txt",
            hints=["`touch {one,two,three}.txt`"],
            solution="cd ~/lab && touch {one,two,three}.txt",
        ),
        Task(
            id="m02-02", title="Каталоги проекта",
            brief="""`mkdir` (make directory) создаёт каталог.

Ключ `-p` (parents) — очень важный:
  mkdir docs             # создаёт docs (если parent существует)
  mkdir a/b/c            # ОШИБКА если a или a/b не существуют
  mkdir -p a/b/c         # создаёт всю цепочку без ошибок

Без `-p` mkdir упадёт с ошибкой при попытке создать вложенный каталог,
если промежуточные каталоги ещё не существуют.

Раскрытие скобок работает и с mkdir:
  mkdir -p project/{src,bin,docs,tests}

В реальной работе структуру проекта создают одной такой командой в начале.
Это быстрее и воспроизводимо — можно скопировать в README или Makefile.

Задание: создай структуру `~/lab/project/` с подкаталогами `src`, `bin`, `docs` — одной командой.""",
            check=f"test -d {LAB}/project/src && test -d {LAB}/project/bin && test -d {LAB}/project/docs",
            hints=["`mkdir -p ~/lab/project/{src,bin,docs}`"],
            solution="mkdir -p ~/lab/project/{src,bin,docs}",
        ),
        Task(
            id="m02-03", title="Копирование",
            brief="""`cp` (copy) — копирование файлов и каталогов.

Синтаксис: `cp [ключи] источник назначение`

Ключи:
  -r    рекурсивно (обязателен для каталогов)
  -a    «архивный» режим: сохраняет права, владельца, время изменения
        (эквивалент -r -p -l)
  -v    verbose: показывает каждый скопированный файл
  -u    копировать только если источник новее назначения
  -n    не перезаписывать существующие файлы

Примеры:
  cp file.txt backup.txt         # копия файла с новым именем
  cp file.txt /tmp/              # в каталог (имя сохраняется)
  cp -r mydir/ /backup/mydir/    # копировать каталог
  cp -av src/ dst/               # с сохранением атрибутов и выводом

При копировании в каталог: если путь назначения заканчивается на `/`
или является существующим каталогом — файл помещается внутрь.

Задание: скопируй `one.txt` в `project/src/`, а весь каталог `project` — в `project-backup`.""",
            check=f"test -f {LAB}/project/src/one.txt && test -d {LAB}/project-backup/src && test -f {LAB}/project-backup/src/one.txt",
            hints=["`cp one.txt project/src/`", "`cp -r project project-backup`"],
            solution="cd ~/lab && cp one.txt project/src/ && cp -r project project-backup",
        ),
        Task(
            id="m02-04", title="Перемещение и переименование",
            brief="""В Linux нет отдельной команды «переименовать» — это просто перемещение
с новым именем. Одна команда `mv` делает оба действия.

Синтаксис: `mv [ключи] источник назначение`

Примеры:
  mv old.txt new.txt          # переименовать
  mv file.txt /tmp/           # переместить в другой каталог
  mv file.txt /tmp/new.txt    # переместить и переименовать одновременно
  mv *.log /var/log/archive/  # переместить несколько файлов

Ключи:
  -v    показывать что перемещается
  -n    не перезаписывать существующий файл
  -b    сделать резервную копию перед перезаписью
  -i    спрашивать перед перезаписью (интерактивный режим)

Важно: `mv` внутри одной файловой системы — мгновенная операция
(просто меняется запись в каталоге). Между ФС — это фактически cp + rm.

Задание: переименуй `two.txt` в `notes.md` и перемести `three.txt` в каталог `project/docs/`.""",
            check=f"test -f {LAB}/notes.md && ! test -f {LAB}/two.txt && test -f {LAB}/project/docs/three.txt",
            hints=["`mv two.txt notes.md`", "`mv three.txt project/docs/`"],
            solution="cd ~/lab && mv two.txt notes.md && mv three.txt project/docs/",
        ),
        Task(
            id="m02-05", title="Удаление (осторожно!)",
            brief="""`rm` удаляет файлы и каталоги. В Linux нет корзины — удалённое не вернуть.

  rm файл           # удалить файл
  rm -r каталог     # удалить каталог рекурсивно (со всем содержимым)
  rm -f файл        # force: не спрашивать, игнорировать ошибки
  rm -rf каталог    # самая опасная комбинация — без вопросов, с каталогом
  rmdir каталог     # удалить только пустой каталог (безопаснее)

Профессиональные привычки:
1. Перед `rm *.log` — сначала запусти `ls *.log` с теми же аргументами.
2. Никогда не запускай `rm -rf /` или `rm -rf ~/` не думая.
3. Для важных данных — сначала архив, потом удаление.
4. `rm -i` (интерактивный) — спрашивает перед каждым файлом.

Паттерны для мусора (маска `*`):
  rm *.tmp           # все файлы с расширением .tmp
  rm -r tmp*/        # каталоги, начинающиеся на tmp

Задание: удали каталог `~/lab/trash` вместе с содержимым и файл `~/lab/junk.tmp`.""",
            setup=f"mkdir -p {LAB}/trash/sub && touch {LAB}/trash/sub/x {LAB}/junk.tmp",
            check=f"! test -e {LAB}/trash && ! test -e {LAB}/junk.tmp",
            hints=["`rm -r trash`", "`rm junk.tmp`"],
            solution="cd ~/lab && rm -r trash && rm junk.tmp",
        ),
        Task(
            id="m02-06", title="Ссылки: жёсткие и символические",
            brief="""В Linux файл на диске — это «inode» с данными. Имя файла — лишь ссылка
на этот inode. Можно создавать несколько имён для одних и тех же данных.

Жёсткая ссылка (`ln цель имя`):
  — Второе имя того же inode (тех же данных на диске)
  — Удаление одного имени не трогает данные, пока есть другие
  — Работает только в пределах одной файловой системы
  — Не работает для каталогов

Символическая ссылка — «симлинк» (`ln -s цель имя`):
  — Отдельный файл, содержащий путь к цели
  — Аналог «ярлыка» в Windows
  — Работает между файловыми системами
  — Может ссылаться на каталоги
  — Если цель удалена — ссылка «висит» (dangling symlink)

  ln -s notes.md latest         # создать симлинк
  ls -l latest                  # покажет: latest -> notes.md
  readlink latest               # покажет путь, на который ссылается
  readlink -f latest            # абсолютный путь цели

Типичное применение: /usr/bin/python -> python3, конфиги Nginx sites-enabled/

Задание: сделай символическую ссылку `~/lab/latest` на файл `~/lab/notes.md`.""",
            check=f"test -L {LAB}/latest && readlink -f {LAB}/latest | grep -q 'notes.md'",
            hints=["`ln -s notes.md latest` (из каталога ~/lab)", "Проверить: `ls -l latest`"],
            solution="cd ~/lab && ln -s notes.md latest",
        ),
    ],
)

m03 = Module(
    id="m03", title="Чтение и создание текстовых файлов", level="новичок",
    tasks=[
        Task(
            id="m03-01", title="cat и heredoc",
            brief="""`cat` (concatenate) — просматривать файлы и создавать их.

Читать файл:
  cat /etc/hostname           # вывести содержимое
  cat -n file.txt             # с номерами строк
  cat file1.txt file2.txt     # объединить несколько файлов

Записать в файл:
  cat > file.txt              # ввод с клавиатуры → файл (Ctrl+D завершает)
  cat >> file.txt             # то же, но дописывает в конец

Heredoc — удобный способ записать многострочный текст в файл:
  cat > poem.txt << 'EOF'
  первая строка
  вторая строка
  третья строка
  EOF

Одинарные кавычки вокруг EOF важны: без них shell будет раскрывать
переменные ($HOME и т.п.) внутри текста.

`printf` — более предсказуемая альтернатива:
  printf 'строка 1\\nстрока 2\\nстрока 3\\n' > file.txt
  \\n — символ новой строки

Задание: создай `~/lab/poem.txt` ровно из трёх непустых строк.""",
            setup=f"rm -f {LAB}/poem.txt",
            check=f"test -f {LAB}/poem.txt && [ \"$(grep -c . {LAB}/poem.txt)\" -eq 3 ]",
            hints=["Можно и так: `printf 'a\\nb\\nc\\n' > poem.txt`"],
            solution="printf 'раз\\nдва\\nтри\\n' > ~/lab/poem.txt",
        ),
        Task(
            id="m03-02", title="head, tail и большие файлы",
            brief="""`cat` большого файла сразу выводит тысячи строк — терминал
засыпает прокруткой. Для больших файлов есть специальные инструменты.

`head` — первые строки:
  head файл         # первые 10 строк (по умолчанию)
  head -n 20 файл   # первые 20 строк
  head -5 файл      # сокращённая форма: первые 5 строк

`tail` — последние строки:
  tail файл         # последние 10 строк
  tail -n 20 файл   # последние 20 строк
  tail -f файл      # режим «follow»: показывает новые строки по мере добавления
                    # незаменимо при чтении логов в реальном времени (Ctrl+C выход)
  tail -F файл      # то же, но переподключается если файл пересоздаётся

Практика: смотреть лог в реальном времени:
  tail -f /var/log/syslog       # следить за системным логом
  tail -f /var/log/nginx/error.log   # следить за Nginx

Получить диапазон строк (строки 40–60):
  sed -n '40,60p' файл
  awk 'NR>=40 && NR<=60' файл

Задание: из подготовленного `~/lab/big.log` (1000 строк) сохрани последние 20 строк в `~/lab/tail20.txt`.""",
            setup=f"seq 1 1000 | sed 's/^/line /' > {LAB}/big.log; rm -f {LAB}/tail20.txt",
            check=f"[ \"$(wc -l < {LAB}/tail20.txt)\" -eq 20 ] && grep -qx 'line 1000' {LAB}/tail20.txt && grep -qx 'line 981' {LAB}/tail20.txt",
            hints=["`tail -n 20 big.log > tail20.txt`"],
            solution="cd ~/lab && tail -n 20 big.log > tail20.txt",
        ),
        Task(
            id="m03-03", title="less — листаем большие файлы",
            brief="""`less` — интерактивная программа просмотра файлов. В отличие от `cat`,
не выводит весь файл сразу, а позволяет листать и искать.

Открытие: `less файл` или `команда | less`

Навигация:
  Стрелки / PgUp / PgDn   листать
  G                        перейти в конец файла
  g                        перейти в начало файла
  q                        выйти

Поиск:
  /текст Enter             найти вперёд
  ?текст Enter             найти назад
  n                        следующее совпадение
  N                        предыдущее совпадение

Режимы:
  -N                       показывать номера строк (can be toggled with -N)
  F                        режим «follow» как у tail -f (Ctrl+C вернуть в less)

Примеры использования:
  less /var/log/syslog             # читать лог
  journalctl | less                # читать лог systemd
  git diff | less                  # читать диффы

`less` умнее `more`: можно листать назад, искать, и программа не
загружает весь файл в память — хорошо работает с файлами любого размера.

Задание: открой `~/lab/big.log` в `less`, найди строку `line 777`, выйди. Затем подтверди проверку.""",
            check_history=r"\bless\b",
            hints=["`less big.log`, затем набери `/line 777` и Enter, выход — `q`"],
            solution="less ~/lab/big.log",
        ),
        Task(
            id="m03-04", title="wc — считаем строки и слова",
            brief="""`wc` (word count) считает строки, слова и байты в файле или потоке.

  wc файл          # строки, слова, байты сразу
  wc -l файл       # только строки (lines) — самый частый вариант
  wc -w файл       # только слова (words)
  wc -c файл       # только байты (characters/bytes)
  wc -m файл       # символы (учитывает многобайтовые, например кириллицу)

Типичные применения:
  wc -l /etc/passwd            # сколько пользователей в системе
  ls /etc | wc -l              # сколько файлов в /etc
  cat access.log | grep 404 | wc -l   # сколько ошибок 404

Тонкость с редиректом:
  wc -l имя_файла       # выводит: "   42 имя_файла" (с именем файла)
  wc -l < имя_файла     # выводит только: "42" (без имени — stdin)

При записи в файл разница важна — если нужно только число, используй `< файл`.

Задание: посчитай, сколько строк в `/etc/passwd`, и запиши только число в `~/lab/users-count.txt`.""",
            check=f"[ \"$(cat {LAB}/users-count.txt 2>/dev/null | tr -d ' ')\" = \"$(wc -l < /etc/passwd)\" ]",
            hints=["`wc -l < /etc/passwd > ~/lab/users-count.txt` — при вводе через `<` имя файла не печатается"],
            solution="wc -l < /etc/passwd > ~/lab/users-count.txt",
        ),
        Task(
            id="m03-05", title="Редактор в терминале",
            brief="""На сервере без графики нужен терминальный текстовый редактор.
Знать хотя бы один — обязательный навык администратора.

`nano` — простой, дружелюбный для начинающих:
  Ctrl+O          сохранить (write Out), затем Enter подтвердить имя
  Ctrl+X          выйти (если есть несохранённые изменения — спросит)
  Ctrl+K          вырезать строку
  Ctrl+U          вставить
  Ctrl+W          поиск
  Ctrl+G          справка

`vim` — мощный, но с непривычным управлением:
  Запускается в «командном» режиме (нельзя просто печатать!)
  i               войти в режим вставки (Insert) — теперь можно печатать
  Esc             вернуться в командный режим
  :w              сохранить (write)
  :q              выйти
  :wq             сохранить и выйти
  :q!             выйти без сохранения (force)
  gg / G          в начало / в конец файла

Если случайно оказался в vim и не знаешь как выйти — нажми Esc несколько
раз, затем наберите `:q!` и Enter.

Задание: создай редактором файл `~/lab/editor.txt` со словом `готово` внутри.""",
            setup=f"rm -f {LAB}/editor.txt",
            check=f"grep -q 'готово' {LAB}/editor.txt",
            hints=["`nano ~/lab/editor.txt`, набери текст, Ctrl+O, Enter, Ctrl+X"],
            solution="nano ~/lab/editor.txt",
        ),
    ],
)

MODULES = [m01, m02, m03]