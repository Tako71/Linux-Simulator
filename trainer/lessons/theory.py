from ..model import Module, Task

# ─────────────────────────────────────────────────────────────────────────────
# M15 — Как запускается Linux
# ─────────────────────────────────────────────────────────────────────────────

m15 = Module(
    id="m15", title="Как запускается Linux: от кнопки до shell", level="теория",
    tasks=[
        Task(
            id="m15-01", title="Шаг 1: BIOS / UEFI и POST",
            theory=True,
            brief="""Когда ты нажимаешь кнопку питания, первым просыпается не Linux,
а прошивка материнской платы — BIOS или UEFI.

POST (Power-On Self-Test):
  Прошивка проверяет железо: RAM, CPU, диски, видеокарта.
  Если что-то не так — сигнал пищалкой или сообщение на экране.

BIOS (Basic Input/Output System):
  Старый стандарт, 16-битный режим, лимит диска 2 ТБ (MBR).
  Ищет загрузочный сектор (MBR — первые 512 байт диска).

UEFI (Unified Extensible Firmware Interface):
  Современная замена BIOS, 64-битная, поддерживает диски >2 ТБ (GPT).
  Имеет свой раздел ESP (EFI System Partition, /boot/efi).
  Может запускать .efi-файлы напрямую без MBR.
  Поддерживает Secure Boot — проверяет подпись загрузчика.

Схема:
  Кнопка питания
    → BIOS/UEFI (POST, инициализация железа)
    → Поиск загрузочного устройства
    → Загрузчик (GRUB)

На сервере UEFI почти всегда. На старом железе и виртуалках —
может быть BIOS (в Docker-контейнере этого нет вообще —
контейнер не проходит загрузку, он просто запускает процессы).

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m15-02", title="Шаг 2: Загрузчик GRUB",
            theory=True,
            brief="""GRUB (Grand Unified Bootloader) — стандартный загрузчик Linux.

Задача GRUB:
  1. Загрузить ядро Linux в память.
  2. Передать ядру аргументы командной строки.
  3. (Опционально) показать меню выбора ОС.

Конфигурация:
  /boot/grub/grub.cfg          основной конфиг (генерируется автоматически)
  /etc/default/grub            параметры для update-grub
  /etc/grub.d/                 скрипты-фрагменты конфига

Изменить параметры ядра:
  1. Редактируешь /etc/default/grub
  2. GRUB_CMDLINE_LINUX_DEFAULT="quiet splash"
  3. Запускаешь update-grub (генерирует grub.cfg)

Важные аргументы ядра (передаются через GRUB):
  root=/dev/sda1    где корневая ФС
  ro               монтировать корень только для чтения (потом rw)
  quiet            не печатать сообщения ядра
  init=/bin/bash   запустить bash вместо systemd (восстановление!)

GRUB rescue — аварийный режим:
  Если GRUB не может найти grub.cfg — ты попадёшь в консоль grub>.
  Отсюда можно вручную указать ядро и загрузить систему.

MBR vs UEFI:
  BIOS/MBR: GRUB живёт в MBR (446 байт) и первых секторах диска.
  UEFI/GPT:  GRUB — .efi-файл в разделе ESP (/boot/efi/EFI/ubuntu/).

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m15-03", title="Шаг 3: Ядро Linux",
            theory=True,
            brief="""GRUB передаёт управление ядру — сжатому файлу vmlinuz.

Что делает ядро при загрузке:
  1. Распаковывает себя в памяти.
  2. Инициализирует CPU, память, базовые подсистемы.
  3. Определяет и инициализирует оборудование (драйверы).
  4. Монтирует initramfs как временную корневую ФС.
  5. Запускает процесс init (PID 1).

Файлы ядра в /boot:
  vmlinuz-6.8.0-45-generic    сжатое ядро (vmlinuz = «виртуальная память Linux»)
  initrd.img-6.8.0-45-generic временная корневая ФС
  System.map-6.8.0-45-generic таблица символов (для отладки)
  config-6.8.0-45-generic      конфиг с которым собрано ядро

Ядро — не монолитная программа. Части загружаются как модули:
  lsmod                 список загруженных модулей
  modprobe имя          загрузить модуль
  modprobe -r имя       выгрузить
  /lib/modules/VERSION/ все доступные модули

Параметры ядра во время работы (/proc и /sys):
  cat /proc/version              версия ядра
  uname -r                       короткая версия
  cat /proc/cmdline              аргументы с которыми запустилось ядро
  sysctl -a                      все параметры ядра
  sysctl net.ipv4.ip_forward     конкретный параметр

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m15-04", title="Шаг 4: initramfs",
            theory=True,
            brief="""Проблема курицы и яйца: чтобы смонтировать корневую ФС,
нужны драйверы диска. Но драйверы находятся на диске...

Решение — initramfs (initial RAM filesystem):
  Маленький архив (cpio + gzip), который GRUB загружает вместе с ядром.
  Содержит минимальный набор драйверов и утилит.
  Монтируется ядром в памяти как временная корневая ФС.

Что происходит:
  1. Ядро монтирует initramfs как / (в памяти).
  2. Запускает /init или /sbin/init внутри initramfs.
  3. Тот загружает нужные модули (драйвер диска, LVM, LUKS, RAID).
  4. Монтирует настоящую корневую ФС.
  5. Выполняет switch_root — переключается на настоящий /.
  6. Запускает /sbin/init (systemd) с настоящего диска.

Посмотреть содержимое initramfs:
  lsinitramfs /boot/initrd.img-$(uname -r) | head -30

Пересобрать initramfs (если добавил модуль или шифрование):
  update-initramfs -u                  # обновить для текущего ядра
  update-initramfs -u -k all           # для всех ядер

Когда нужно пересобирать:
  — Добавил поддержку шифрования (LUKS)
  — Поменял разметку дисков (LVM)
  — Сменил корневую ФС или UUID
  — Добавил RAID

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m15-05", title="Шаг 5: systemd, PID 1 и targets",
            theory=True,
            brief="""После switch_root ядро запускает /sbin/init — это systemd, PID 1.
Всё остальное в системе — потомок PID 1.

systemd читает конфигурацию и запускает цепочку targets:

  sysinit.target           базовая инициализация системы
    → basic.target         сокеты, таймеры, базовые службы
      → network.target     сетевые интерфейсы
        → multi-user.target  полноценная работа без GUI
          → graphical.target  + дисплейный менеджер (если нужен GUI)

Аналог SysV runlevels:
  runlevel 0 = poweroff.target    выключение
  runlevel 1 = rescue.target      однопользовательский режим
  runlevel 3 = multi-user.target  нормальная работа без GUI
  runlevel 5 = graphical.target   нормальная работа с GUI
  runlevel 6 = reboot.target      перезагрузка

Просмотр загрузки:
  systemd-analyze                  общее время загрузки
  systemd-analyze blame            кто сколько занял
  systemd-analyze critical-chain   критический путь

Типичный вывод systemd-analyze:
  Startup finished in 1.2s (firmware) + 2.1s (loader) + 3.4s (kernel)
                         + 4.1s (userspace) = 10.8s

Полная схема загрузки:
  Кнопка питания
    UEFI/BIOS (POST)
    GRUB (выбор ядра, параметры)
    Ядро Linux (инициализация железа)
    initramfs (загрузка драйверов, монтирование /)
    PID 1 = systemd
    sysinit → basic → network → multi-user
    Логин-менеджер / getty / SSH

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m15-06", title="Восстановление: когда система не загружается",
            theory=True,
            brief="""Практические знания: что делать если система не стартует.

Сценарий 1: GRUB не найден / повреждён
  Загрузись с Live USB (Ubuntu/Debian).
  Смонтируй корневую ФС: mount /dev/sda1 /mnt
  Войди в chroot: chroot /mnt
  Переустанови GRUB: grub-install /dev/sda && update-grub

Сценарий 2: Ядро паникует (kernel panic)
  Читай сообщение паники — там причина.
  Часто: повреждён initramfs или нет модуля для корневой ФС.
  Загрузись со старым ядром через меню GRUB.
  Пересобери: update-initramfs -u -k all

Сценарий 3: systemd не запускается
  В меню GRUB нажми e, добавь к строке linux:
    init=/bin/bash
  Система загрузится прямо в bash без systemd.
  Перемонтируй / для записи: mount -o remount,rw /
  Чини конфиги, потом reboot -f

Сценарий 4: Забыл пароль root
  Аналогично: init=/bin/bash → mount -o remount,rw / → passwd root

Сценарий 5: ФС повреждена
  Ядро само переключит ФС в read-only при ошибках (emergency.target).
  Загрузись с Live USB или в recovery mode.
  Запусти fsck: fsck /dev/sda1 (только на unmounted разделе!)

Rescue mode и Emergency mode:
  systemctl rescue      однопользовательский + монтированные ФС
  systemctl emergency   минимум + только corневая ФС (может быть ro)

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

# ─────────────────────────────────────────────────────────────────────────────
# M16 — Процессы под капотом
# ─────────────────────────────────────────────────────────────────────────────

m16 = Module(
    id="m16", title="Процессы под капотом: fork, зомби, демоны", level="теория",
    tasks=[
        Task(
            id="m16-01", title="Как рождается процесс: fork и exec",
            theory=True,
            brief="""В Linux новый процесс создаётся двумя системными вызовами: fork и exec.

fork() — клонирование:
  Вызывающий процесс (родитель) создаёт точную копию себя (потомок).
  Потомок получает новый PID.
  Оба процесса продолжают выполнение ПОСЛЕ fork — в разных процессах.
  fork() возвращает разные значения: родителю — PID потомка, потомку — 0.

exec() — замена образа:
  Потомок после fork вызывает exec(), чтобы загрузить новую программу.
  exec заменяет код, данные, стек процесса новой программой.
  PID остаётся тот же!

Схема запуска команды в bash:
  bash           ←  твой shell (родитель)
  bash.fork()    →  создаёт копию bash (потомок)
  потомок.exec("ls") →  ls запускается вместо bash-потомка
  bash.wait()    ←  ждёт завершения потомка
  ls завершается →  bash возвращает приглашение

Посмотреть дерево процессов:
  pstree           # дерево всех процессов
  pstree -p        # с PID
  pstree -p $$     # дерево от текущего shell

Каждый процесс знает своего родителя:
  cat /proc/$$/status | grep -E 'Pid|PPid'
  ps -o pid,ppid,cmd   # PID, PPID и командная строка

Наследование:
  Потомок наследует от родителя: переменные окружения, файловые
  дескрипторы (включая открытые файлы!), права доступа, рабочий каталог.

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m16-02", title="Зомби-процессы",
            theory=True,
            brief="""Зомби (zombie, defunct) — это процесс, который завершился,
но его «запись» в таблице процессов ещё не убрана.

Как возникает зомби:
  1. Потомок завершается (exit).
  2. Ядро освобождает ресурсы (память, файлы), но ОСТАВЛЯЕТ запись в таблице.
  3. Родитель должен «подобрать» код завершения через wait().
  4. Пока родитель не вызвал wait() — потомок висит как зомби.

Зомби в выводе ps:
  USER  PID %CPU %MEM  ...  STAT  COMMAND
  root  1234  0.0  0.0  ...   Z   [defunct]
  Буква Z в столбце STAT = Zombie.

Опасность зомби:
  Сам зомби НЕ потребляет память или CPU.
  Но занимает запись в таблице процессов (PID).
  Если таблица переполнится — новые процессы создать невозможно.
  Система встанет.

Как убить зомби:
  Убить зомби напрямую НЕЛЬЗЯ — он уже мёртв.
  Нужно починить родителя:
  1. Послать родителю SIGCHLD: kill -CHLD <PID_родителя>
     (намекнуть «подбери потомка»)
  2. Убить самого родителя: kill <PID_родителя>
     (тогда зомби усыновит init/PID1, который сразу уберёт его)

Найти зомби:
  ps aux | grep Z
  ps -eo pid,ppid,stat,comm | awk '$3 ~ /Z/'

Правильное поведение в коде:
  Программы должны вызывать waitpid() или обрабатывать SIGCHLD.
  systemd и другие «сервисные менеджеры» делают это правильно.

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m16-03", title="Сироты и демоны",
            theory=True,
            brief="""Сирота (orphan process) — процесс, чей родитель умер первым.

Что происходит с сиротой:
  ОС автоматически «усыновляет» его — новым родителем становится PID 1 (systemd).
  Это нормально и безопасно. Сирота продолжает работать.

  Пример: запустил nohup скрипт &, закрыл терминал — скрипт стал сиротой,
  его усыновил systemd, он продолжает работать.

Демон (daemon) — фоновый процесс-сервис.
Традиционный способ создать демона (старый стиль):
  1. fork() — отделиться от терминала
  2. setsid() — создать новую сессию (отвязаться от управляющего терминала)
  3. fork() ещё раз — гарантировать что не является лидером сессии
  4. Закрыть stdin/stdout/stderr, перенаправить в /dev/null
  5. Сменить рабочий каталог на /
  6. Записать PID в файл (pidfile) /var/run/имя.pid

Современный способ: systemd делает всё это сам.
  Просто напиши [Service] ExecStart=... — systemd сам управляет жизнью процесса.

Характеристики демона:
  — PPID = 1 (усыновлён systemd)
  — нет управляющего терминала (TTY = ?)
  — обычно работает от системного пользователя (не root)

Посмотреть демоны:
  ps aux | awk '$7 == "?" {print}'   # TTY = ? означает нет терминала
  systemctl list-units --type=service --state=running

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m16-04", title="/proc — окно в ядро",
            theory=True,
            brief="""`/proc` — псевдофайловая система. Файлов на диске нет — ядро генерирует
содержимое на лету при чтении. Это главный способ заглянуть «внутрь» ОС.

Информация о системе:
  /proc/version        версия ядра и компилятора
  /proc/cpuinfo        подробно о каждом ядре CPU
  /proc/meminfo        детальная информация о памяти
  /proc/uptime         время работы системы (в секундах)
  /proc/loadavg        загрузка (те самые три числа из uptime)
  /proc/mounts         смонтированные ФС
  /proc/net/dev        статистика сетевых интерфейсов
  /proc/net/tcp        TCP-соединения (ss/netstat читают отсюда)
  /proc/sys/           настраиваемые параметры ядра (через sysctl)
  /proc/cmdline        аргументы с которыми запустили ядро

Информация о каждом процессе /proc/PID/:
  /proc/1/status       полная информация о PID 1
  /proc/1/cmdline      командная строка (через \0)
  /proc/1/environ      переменные окружения (через \0)
  /proc/1/fd/          открытые файловые дескрипторы (симлинки)
  /proc/1/maps         карта памяти процесса
  /proc/1/exe          симлинк на исполняемый файл
  /proc/1/cwd          симлинк на рабочий каталог
  /proc/self/          всегда указывает на текущий процесс

Практика:
  ls -l /proc/$$/fd          # мои открытые файлы
  cat /proc/$$/cmdline | tr '\\0' ' '   # моя командная строка
  cat /proc/sys/net/ipv4/ip_forward     # включена ли маршрутизация
  echo 1 > /proc/sys/net/ipv4/ip_forward  # включить (до перезагрузки)

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m16-05", title="Сигналы: полная картина",
            theory=True,
            brief="""Сигнал — асинхронное уведомление процессу о событии. Аналог «прерывания»
на уровне процесса.

Полный список основных сигналов:
  1   SIGHUP    «терминал закрылся» / «перечитай конфиг» (reload)
  2   SIGINT    Ctrl+C — прерывание (interrupt)
  3   SIGQUIT   Ctrl+\\ — выход с core dump
  9   SIGKILL   принудительное уничтожение (нельзя заблокировать!)
  10  SIGUSR1   пользовательский сигнал 1 (смысл задаёт программа)
  12  SIGUSR2   пользовательский сигнал 2
  15  SIGTERM   вежливое завершение (по умолчанию у kill)
  17  SIGCHLD   потомок завершился или остановился
  18  SIGCONT   продолжить после SIGSTOP
  19  SIGSTOP   принудительная пауза (нельзя заблокировать!)
  20  SIGTSTP   Ctrl+Z — пауза (можно заблокировать)

Что процесс может сделать с сигналом:
  1. Выполнить обработчик (handler) — своя реакция.
  2. Игнорировать (кроме SIGKILL и SIGSTOP).
  3. Принять действие по умолчанию (обычно завершение).

SIGKILL и SIGSTOP нельзя ни перехватить, ни заблокировать, ни игнорировать.
Именно поэтому kill -9 всегда работает.

Реальные применения:
  kill -HUP $(pgrep nginx)    перечитать конфиг nginx без перезапуска
  kill -USR1 $(pgrep apache2) ротировать логи Apache
  kill -STOP <PID>            заморозить процесс (без уничтожения)
  kill -CONT <PID>            разморозить
  trap 'cleanup' SIGTERM      в bash-скриптах: поймать сигнал завершения

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

# ─────────────────────────────────────────────────────────────────────────────
# M17 — Топ вопросов с Linux-собеседований
# ─────────────────────────────────────────────────────────────────────────────

m17 = Module(
    id="m17", title="Топ вопросов с Linux-собеседований", level="теория",
    tasks=[
        Task(
            id="m17-01", title="Что происходит когда ты вводишь команду в bash?",
            theory=True,
            brief="""Классический вопрос. Полный ответ показывает глубину понимания.

Ты набрал: ls -la /tmp

1. Bash читает строку и разбивает на токены:
   команда: ls,  аргументы: -la, /tmp

2. Bash проверяет тип:
   — Встроенная команда bash? (cd, echo, export...) → выполняет сам.
   — Алиас? → подставляет.
   — Функция? → вызывает.
   — Иначе → ищет в PATH.

3. Поиск в PATH:
   PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
   Bash проверяет каждый каталог: /usr/local/sbin/ls? Нет. /usr/bin/ls? Да!
   (Результат кешируется в hash-таблице bash.)

4. Подготовка:
   Bash выполняет перенаправления (>, >>, |, <) если есть.
   Раскрывает глобы (* ? []) если есть.
   Раскрывает переменные ($HOME, $USER...).

5. fork() — bash клонирует себя.

6. В потомке: exec("/usr/bin/ls", ["-la", "/tmp"])
   Образ bash заменяется образом ls.

7. ls выполняется, пишет в stdout.

8. Родитель-bash вызывает wait() — ждёт завершения ls.

9. ls завершается, bash получает код возврата ($?).

10. Bash возвращает приглашение PS1.

Вся эта цепочка занимает миллисекунды.

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-02", title="Inode, hard link и symlink: в чём разница?",
            theory=True,
            brief="""Один из самых частых вопросов на собеседованиях по Linux.

Inode — структура данных на диске с метаданными файла:
  — Размер, права, владелец, группа
  — Время создания/изменения/доступа
  — Счётчик жёстких ссылок
  — Указатели на блоки данных
  НЕ содержит имя файла! Имя хранится в каталоге.

Каталог — это просто таблица: имя → номер inode.

Hard link (жёсткая ссылка):
  — Ещё одна запись в каталоге, указывающая на ТОТ ЖЕ inode
  — Оба имени равноправны: «оригинала» нет
  — Данные удаляются только когда счётчик ссылок = 0
  — Нельзя: между ФС, для каталогов (кроме . и ..)
  ln file1.txt file2.txt     # file2.txt — hard link

Symbolic link (симлинк, мягкая ссылка):
  — Отдельный файл с отдельным inode
  — Содержимое файла = путь к цели (строка)
  — При доступе ядро «следует» по ссылке
  — Можно: между ФС, для каталогов
  — Если цель удалена — «битая» ссылка (dangling)
  ln -s /path/to/target link   # создать симлинк

Проверить:
  ls -li file1.txt file2.txt    # одинаковый inode → hard link
  ls -l symlink                 # symlink -> /path/target
  stat file.txt                 # Links: N — счётчик жёстких ссылок

Практическое применение:
  Hard links: /usr/bin/vi → /usr/bin/vim (одно и то же)
  Symlinks: /etc/alternatives/python → python3 (переключаемые версии)
            nginx sites-enabled/ → sites-available/mysite.conf

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-03", title="Как работает pipe? Что такое /dev/null?",
            theory=True,
            brief="""Pipe (конвейер):

`ls | grep txt` — как это работает внутри?

  1. Ядро создаёт pipe — буфер в памяти с двумя концами:
     write-end (для записи) и read-end (для чтения).
  2. bash fork()-ает дважды: создаёт два дочерних процесса.
  3. Первый потомок (ls): stdout → write-end пайпа. Выполняет exec(ls).
  4. Второй потомок (grep): stdin → read-end пайпа. Выполняет exec(grep).
  5. ls пишет в пайп, grep читает оттуда — синхронно.
     Пайп блокирует ls если буфер полон, grep если буфер пуст.
  6. bash wait()-ает оба процесса.

Размер буфера пайпа: 65536 байт (64 КБ) по умолчанию.

Named pipe (FIFO):
  mkfifo mypipe         # создать именованный пайп
  cat file > mypipe &   # писать в него в фоне
  cat < mypipe          # читать из него
  ls -l mypipe          # тип 'p'

/dev/null — устройство «чёрная дыра»:
  Запись: всё записанное немедленно отбрасывается.
  Чтение: сразу EOF (пустой файл).
  command > /dev/null 2>&1   # выбросить весь вывод

/dev/zero — бесконечный источник нулевых байт:
  dd if=/dev/zero of=file bs=1M count=100   # создать файл 100 МБ из нулей
  head -c 1M /dev/zero | wc -c             # 1048576

/dev/random и /dev/urandom — генераторы случайных чисел:
  /dev/random    блокирует пока не накопится энтропия (медленнее)
  /dev/urandom   не блокирует (быстрее, криптографически безопасен)
  head -c 16 /dev/urandom | base64    # случайная строка

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-04", title="Swap и виртуальная память",
            theory=True,
            brief="""Виртуальная память:
Каждый процесс видит своё адресное пространство — как будто у него вся память.
Ядро отображает виртуальные адреса на физические страницы (4 КБ).

Что это даёт:
  — Изоляция: процесс не видит память других процессов.
  — Больше памяти, чем физически есть (за счёт swap).
  — Shared libraries: одна копия libc в памяти, видна всем процессам.
  — Copy-on-write после fork: страницы копируются только при записи.

Swap — раздел или файл на диске, используемый как «дополнительная RAM»:
  Если физическая RAM заполнена → ядро «вытесняет» страницы на диск.
  При обращении к вытесненной странице → page fault → загрузка с диска.
  Диск в ~100 раз медленнее RAM → swap = лаги.

Посмотреть состояние памяти:
  free -h                      # RAM и swap
  cat /proc/meminfo            # подробно
  vmstat 1 5                   # статистика виртуальной памяти (раз в 1с, 5 раз)

Swappiness — насколько охотно использовать swap:
  cat /proc/sys/vm/swappiness  # по умолчанию 60
  sysctl vm.swappiness=10      # меньше = реже использовать swap (для серверов)

OOM Killer (Out-Of-Memory Killer):
  Если памяти не хватает даже со swap → ядро убивает процессы.
  Смотреть: dmesg | grep -i 'killed process'
  Каждый процесс имеет «OOM score» (0–1000): чем выше, тем вероятнее убийство.
  cat /proc/PID/oom_score

На серверах swap обычно = RAM или отключён совсем (Kubernetes требует отключить).

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-05", title="Разница: процесс, поток, горутина",
            theory=True,
            brief="""Частый вопрос на собеседованиях, особенно для DevOps/SRE.

Процесс (process):
  — Независимый экземпляр программы.
  — Своё адресное пространство (память изолирована).
  — Своя таблица файловых дескрипторов.
  — Коммуникация: pipe, socket, shared memory, сигналы.
  — Создание: fork() — дорого (копирует адресное пространство).
  — Изоляция: полная. Крash одного не влияет на другие.

Поток (thread, нить):
  — «Лёгкий процесс» внутри процесса.
  — РАЗДЕЛЯЕТ адресное пространство с другими потоками процесса.
  — Каждый поток имеет свой стек и регистры.
  — Коммуникация: через разделяемую память (очень быстро).
  — Создание: pthread_create() — дешевле fork.
  — Проблема: race conditions, deadlocks. Нужна синхронизация (mutex).
  — Crash одного потока часто убивает весь процесс.

В Linux потоки реализованы через те же структуры что и процессы (clone()),
просто с флагом «разделять память». Поэтому `ps aux` показывает потоки.

Посмотреть потоки процесса:
  ps -T -p PID         # потоки процесса PID
  ls /proc/PID/task/   # каждая директория = один поток (TID)

Горутина (Go), корутина (Python async):
  — «Зелёные потоки» — управляются runtime языка, не ОС.
  — Тысячи на одном потоке ОС (мультиплексирование).
  — Очень лёгкое переключение контекста (без syscall).
  — Go: go func() {}
  — Python: async/await, asyncio

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-06", title="Сеть: TCP handshake и состояния сокета",
            theory=True,
            brief="""Понимание TCP нужно для диагностики сетевых проблем.

TCP three-way handshake (установка соединения):
  Клиент → Сервер:  SYN (seq=X)
  Сервер → Клиент:  SYN-ACK (seq=Y, ack=X+1)
  Клиент → Сервер:  ACK (ack=Y+1)
  Соединение установлено!

Состояния TCP-сокета (ss -tan):
  LISTEN        сервер ждёт подключений
  SYN_SENT      клиент отправил SYN, ждёт SYN-ACK
  SYN_RECV      сервер получил SYN, отправил SYN-ACK
  ESTABLISHED   соединение установлено, идёт обмен данными
  FIN_WAIT_1    отправили FIN (начало разрыва)
  FIN_WAIT_2    получили ACK на FIN, ждём FIN от другой стороны
  TIME_WAIT     ждём 2*MSL чтобы убедиться что все пакеты дошли
  CLOSE_WAIT    получили FIN, но наше приложение ещё не закрыло сокет
  LAST_ACK      отправили FIN, ждём последнего ACK
  CLOSED        соединение закрыто

Проблемы:
  Много TIME_WAIT → сервер закрывает много соединений (норма для HTTP/1.0)
  Много CLOSE_WAIT → утечка сокетов в приложении (не вызывает close())
  Много SYN_RECV → SYN-flood атака или медленный бэкенд

Полезные команды:
  ss -tan state established   # только установленные соединения
  ss -tan | awk '{print $1}' | sort | uniq -c   # статистика по состояниям
  ss -tn dst :443             # соединения на 443 порт

/proc/sys/net/ipv4/tcp_tw_reuse=1  — переиспользовать TIME_WAIT сокеты

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-07", title="Права, setuid и sticky bit",
            theory=True,
            brief="""Помимо rwx существуют три специальных бита.

setuid (SUID) — бит на исполняемых файлах:
  Файл запускается с правами ВЛАДЕЛЬЦА, а не запускающего.
  В ls -l выглядит как s вместо x у владельца: -rwsr-xr-x
  Числовой код: 4xxx (например 4755)

  chmod u+s /usr/bin/passwd    # задать SUID
  Пример: /usr/bin/passwd имеет SUID root — позволяет любому менять свой
  пароль, хотя для записи в /etc/shadow нужен root.

  ВНИМАНИЕ: SUID на скриптах bash игнорируется ядром (риск безопасности).

setgid (SGID) — бит на файлах и каталогах:
  На файле: запускается с правами ГРУППЫ владельца.
  На каталоге: новые файлы внутри наследуют группу каталога.
  Выглядит как s вместо x у группы: -rwxr-sr-x
  Числовой код: 2xxx

  chmod g+s /shared/project/   # все новые файлы → группа project

Sticky bit — бит на каталогах:
  Файлы в каталоге может удалить только ВЛАДЕЛЕЦ файла (или root).
  Выглядит как t вместо x у остальных: drwxrwxrwt
  Числовой код: 1xxx

  /tmp имеет sticky bit: все могут писать, но удалить можно только своё.
  ls -ld /tmp     → drwxrwxrwt

  chmod +t /shared/uploads/    # защитить каталог загрузок

Найти все SUID файлы в системе (полезно для аудита безопасности):
  find / -perm -4000 -type f 2>/dev/null

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m17-08", title="Диагностика производительности: что смотреть",
            theory=True,
            brief="""Система «тормозит» — с чего начать? Системный подход.

Определи узкое место по чеклисту:

1. CPU:
  top / htop          — %CPU, load average
  mpstat 1 5          — загрузка каждого ядра
  vmstat 1 5          — столбцы r (run queue) и b (blocked)
  Признак: load average >> числа CPU, %user или %sys высокие

2. Память:
  free -h             — available < 10% RAM = проблема
  vmstat 1 5          — si/so > 0 = активный swap (катастрофа)
  top, сортировка M   — кто ест память

3. Диск (I/O):
  iostat -xz 1 5      — %util > 80% = диск перегружен
  iotop               — какой процесс виновен
  vmstat 1 5          — столбец b > 0 = процессы ждут I/O

4. Сеть:
  iftop / nethogs     — кто генерирует трафик
  ss -tn              — состояния соединений
  ip -s link          — ошибки интерфейса (errors, dropped)

5. Конкретный процесс:
  strace -p PID       — системные вызовы в реальном времени
  lsof -p PID         — открытые файлы и сокеты
  /proc/PID/status    — детали состояния

Один инструмент — всё сразу:
  dstat 1             # CPU, disk, net, mem в одной таблице

Золотое правило диагностики: сначала найди узкое место,
потом ищи виновника, потом решай. Не угадывай.

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)


# ─────────────────────────────────────────────────────────────────────────────
# M18 — Файловые системы под капотом
# ─────────────────────────────────────────────────────────────────────────────

m18 = Module(
    id="m18", title="Файловые системы: ext4, LVM, RAID", level="теория",
    tasks=[
        Task(
            id="m18-01", title="Как устроена файловая система ext4",
            theory=True,
            brief="""ext4 — стандартная ФС для большинства Linux-дистрибутивов.

Основные структуры на диске:

Superblock — «паспорт» ФС:
  Хранит: размер блока, число inodes, число блоков, время последнего монтирования.
  Дублируется в нескольких местах (защита от повреждения).
  tune2fs -l /dev/sda1    # прочитать superblock

Block groups — ФС делится на группы блоков:
  Каждая группа содержит: bitmap блоков, bitmap inodes, таблицу inodes, данные.
  Цель: данные файла и его inode — физически рядом (быстрее читать).

Inode table — массив структур inode:
  Размер inode по умолчанию: 256 байт.
  Хранит: права, владелец, размер, временны́е метки, указатели на блоки.
  НЕ хранит: имя файла (оно в записи каталога).

Extent tree — как inode ссылается на данные:
  Вместо прямых указателей на блоки ext4 использует extents:
  Extent = {начальный блок, длина} — описывает непрерывный участок.
  Файл из 1000 последовательных блоков = 1 extent (вместо 1000 указателей).

Журнал (journal):
  Перед записью ext4 сначала пишет в журнал, потом на место.
  При сбое питания — перечитывает журнал и доигрывает незавершённые операции.
  Это и есть «журналирование» (journaling).
  Без него после сбоя нужен длинный fsck.

Проверить ФС:
  dumpe2fs /dev/sda1 | head -50    # полная информация о ФС
  e2fsck -n /dev/sda1              # проверить без исправлений
  tune2fs -l /dev/sda1             # краткая информация

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m18-02", title="Другие файловые системы: XFS, Btrfs, tmpfs",
            theory=True,
            brief="""Выбор ФС важен для производительности и надёжности.

XFS:
  Разработана SGI, оптимизирована для больших файлов и параллельного I/O.
  Отлично масштабируется на многоядерных системах и массивах.
  Стандарт для RHEL/CentOS. Нельзя уменьшить (только расширить).
  Применение: медиасерверы, базы данных, хранилища.

Btrfs (B-tree FS):
  Copy-on-write: запись не перезаписывает, а пишет в новое место.
  Снапшоты (snapshots): мгновенные, почти бесплатные по месту.
  Проверка данных (checksums): обнаруживает битые данные.
  RAID встроен. Можно добавлять диски без остановки.
  Применение: десктоп, Fedora, openSUSE по умолчанию.

ZFS (через OpenZFS):
  Исходно Sun Solaris. Самая надёжная ФС.
  Copy-on-write + checksums + RAID-Z + снапшоты + дедупликация.
  Защита от «тихого повреждения данных» (silent corruption).
  Применение: NAS, хранилище данных, FreeBSD.
  На Linux: apt install zfsutils-linux

tmpfs — ФС в памяти:
  Файлы хранятся в RAM (и swap). При перезагрузке — исчезают.
  Скорость: RAM = без задержек диска.
  /tmp и /run — обычно tmpfs.
  mount -t tmpfs -o size=512M tmpfs /mnt/fast   # создать вручную
  df -h /tmp   # посмотреть размер

Сравнение скорости (условно):
  tmpfs > XFS ≈ ext4 > Btrfs (при большой нагрузке)
  По надёжности: ZFS > Btrfs > ext4/XFS ≈ (все хороши)

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m18-03", title="LVM: гибкое управление дисками",
            theory=True,
            brief="""LVM (Logical Volume Manager) — слой абстракции над дисками.
Позволяет изменять размеры разделов «на лету», без перезагрузки.

Три уровня LVM:

PV (Physical Volume) — физический диск или раздел:
  pvcreate /dev/sdb              # инициализировать диск для LVM
  pvdisplay                      # показать PV
  pvs                            # краткий список

VG (Volume Group) — группа PV, один большой «пул»:
  vgcreate data /dev/sdb /dev/sdc   # создать VG из двух дисков
  vgdisplay data                    # информация о VG
  vgs                               # краткий список
  vgextend data /dev/sdd            # добавить диск в группу

LV (Logical Volume) — логический «раздел» из VG:
  lvcreate -L 50G -n mysql data     # создать LV 50 ГБ
  lvcreate -l 100%FREE -n backup data  # использовать всё свободное
  lvdisplay                          # информация о LV
  lvs                                # краткий список

Использование LV как диска:
  mkfs.ext4 /dev/data/mysql          # создать ФС
  mount /dev/data/mysql /var/lib/mysql

Расширить LV (без остановки):
  lvextend -L +20G /dev/data/mysql          # добавить 20 ГБ
  resize2fs /dev/data/mysql                 # расширить ФС (ext4)
  xfs_growfs /var/lib/mysql                 # для XFS

Снапшот (мгновенная копия):
  lvcreate -L 5G -s -n mysql-snap /dev/data/mysql
  # снапшот занимает только изменившиеся блоки

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m18-04", title="RAID в Linux (mdadm)",
            theory=True,
            brief="""RAID (Redundant Array of Independent Disks) — объединение дисков
для надёжности и/или производительности.

Основные уровни:

RAID 0 (striping — чередование):
  Данные делятся на полосы, пишутся на все диски параллельно.
  Производительность: в N раз лучше чтения И записи.
  Надёжность: ХУЖЕ одного диска. Сломается хоть один — данные потеряны.
  Применение: кеш, временные данные.

RAID 1 (mirroring — зеркало):
  Каждый диск — точная копия другого.
  Производительность: чтение быстрее (с двух дисков), запись = 1 диск.
  Надёжность: переживёт падение N-1 дисков.
  Применение: системный раздел, критичные данные.

RAID 5 (striping + distributed parity):
  Данные + контрольные суммы распределены по всем дискам.
  Нужно минимум 3 диска. Переживёт потерю 1 диска.
  Ёмкость: (N-1) × размер диска.
  Производительность записи: медленнее из-за parity.

RAID 6:
  Как RAID 5 но два диска parity. Переживёт потерю 2 дисков.
  Нужно минимум 4 диска.

RAID 10 (1+0):
  Зеркало из чередований. Нужно чётное число дисков (мин. 4).
  Производительность RAID 0 + надёжность RAID 1.

Software RAID в Linux (mdadm):
  mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb /dev/sdc
  cat /proc/mdstat              # состояние массива
  mdadm --detail /dev/md0       # подробно

После сбоя диска:
  mdadm /dev/md0 --fail /dev/sdb    # пометить диск сломанным
  mdadm /dev/md0 --remove /dev/sdb  # извлечь
  mdadm /dev/md0 --add /dev/sdd     # добавить новый → ребилд начнётся сам

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m18-05", title="Диагностика дисков: S.M.A.R.T. и I/O",
            theory=True,
            brief="""S.M.A.R.T. (Self-Monitoring, Analysis and Reporting Technology):
Встроенная самодиагностика дисков (HDD и SSD).

  apt install smartmontools
  smartctl -a /dev/sda           # полный отчёт
  smartctl -H /dev/sda           # только вердикт (PASSED/FAILED)
  smartctl -t short /dev/sda     # запустить короткий тест (~2 мин)
  smartctl -t long /dev/sda      # длинный тест (~часы)

Критичные атрибуты S.M.A.R.T.:
  Reallocated_Sector_Ct   > 0 → диск переназначает плохие секторы (тревога!)
  Pending_Sector_Count    > 0 → сектора ожидают переназначения
  Uncorrectable_Sector    > 0 → нечитаемые сектора (критично!)
  Power_On_Hours          → сколько часов работает диск
  Temperature             → температура (>55°C опасно для HDD)

Мониторинг I/O в реальном времени:

iostat (из sysstat):
  iostat -xz 1       # каждую секунду
  %util > 80%        → диск перегружен
  await > 20ms       → высокая задержка (для HDD норма 5-15ms, SSD < 1ms)
  r/s, w/s           → операций чтения/записи в секунду

iotop:
  iotop -o           # только активные процессы
  Показывает кто именно генерирует I/O

Найти медленные файловые операции:
  iowait в vmstat / iostat показывает % времени CPU ждёт I/O
  Высокий iowait = узкое место — диск

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

# ─────────────────────────────────────────────────────────────────────────────
# M19 — Безопасность Linux
# ─────────────────────────────────────────────────────────────────────────────

m19 = Module(
    id="m19", title="Безопасность Linux: capabilities, PAM, аудит", level="теория",
    tasks=[
        Task(
            id="m19-01", title="Модель безопасности Linux: DAC и MAC",
            theory=True,
            brief="""Linux реализует два уровня контроля доступа.

DAC (Discretionary Access Control) — дискреционный:
  Стандартная модель Linux: пользователь сам контролирует свои файлы.
  Права rwx + владелец + группа.
  Слабость: если процесс скомпрометирован — он имеет все права своего пользователя.
  Нет ограничений «сверху»: root всесилен.

MAC (Mandatory Access Control) — мандатный:
  Политики задаёт администратор, пользователи не могут их обойти.
  Даже root ограничен.
  Реализации: SELinux, AppArmor.

SELinux (Security-Enhanced Linux):
  Разработан NSA, используется в RHEL/CentOS/Fedora.
  Каждому объекту присвоена метка (label): user:role:type:level
  Политика = разрешённые взаимодействия между типами.
  Режимы: enforcing (блокирует), permissive (только логирует), disabled.

  getenforce             # текущий режим
  setenforce 0           # переключить в permissive (временно)
  ls -Z /etc/passwd      # метка файла
  ps -Z -p 1             # метка процесса
  audit2allow            # создать политику из логов отказов

AppArmor:
  Используется в Ubuntu/Debian. Проще SELinux.
  Политики привязаны к путям (не к меткам).
  Профили в /etc/apparmor.d/

  aa-status                   # статус AppArmor
  aa-complain /usr/sbin/nginx # режим «жалоб» (logonly)
  aa-enforce /usr/sbin/nginx  # строгий режим

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m19-02", title="Linux Capabilities: разделение root",
            theory=True,
            brief="""Традиционно: либо у тебя все права root, либо ничего.
Capabilities разбивают «суперсилу» root на ~40 отдельных прав.

Зачем это нужно:
  nginx нужен порт 80 (< 1024 = привилегированный).
  Без capabilities — нужно запускать nginx от root (опасно!).
  С capabilities — даём только CAP_NET_BIND_SERVICE (и ничего лишнего).

Основные capabilities:
  CAP_NET_BIND_SERVICE   слушать порты < 1024
  CAP_NET_ADMIN          сетевая конфигурация (ip route, iptables)
  CAP_SYS_ADMIN          «джокер»: много всего (монтирование, namespace и др.)
  CAP_SYS_PTRACE         трассировка процессов (strace, gdb)
  CAP_CHOWN              менять владельца файлов
  CAP_KILL               посылать сигналы любым процессам
  CAP_DAC_OVERRIDE       обходить проверку прав на файлы
  CAP_SETUID/SETGID      менять UID/GID процесса

Посмотреть capabilities:
  capsh --print              # capabilities текущего shell
  cat /proc/self/status | grep Cap   # в hex (нужен capsh для декодирования)
  getcap /usr/bin/ping       # capabilities файла
  getpcaps PID               # capabilities процесса

Назначить capability файлу:
  setcap cap_net_bind_service=+ep /usr/bin/node
  # теперь node может слушать порт 80 без root
  setcap -r /usr/bin/node    # убрать capabilities

Три набора capabilities процесса:
  Permitted   — максимум, что процесс может взять
  Effective   — что реально используется сейчас
  Inheritable — что передаётся дочерним процессам

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m19-03", title="PAM: Pluggable Authentication Modules",
            theory=True,
            brief="""PAM — стандарт аутентификации в Linux. Любая программа (login,
sshd, sudo, su) не реализует аутентификацию сама — она вызывает PAM.

Зачем это хорошо:
  Меняешь правила один раз в PAM — меняется поведение всех программ.
  Хочешь двухфакторную аутентификацию для sudo? Одна строчка в PAM.

Конфигурация: /etc/pam.d/
  /etc/pam.d/sshd       # правила для SSH
  /etc/pam.d/sudo       # правила для sudo
  /etc/pam.d/login      # правила для консольного входа
  /etc/pam.d/common-*   # общие правила (включаются через @include)

Формат строки:
  тип  управление  модуль  аргументы

Тип (что проверяем):
  auth        аутентификация (пароль, токен)
  account     проверка учётки (не истекла, не заблокирована)
  password    смена пароля
  session     действия при входе/выходе (mount домашнего каталога)

Управление (что делать с результатом):
  required    обязательно. Ошибка — в конечном итоге отказ, но остальные модули тоже выполняются
  requisite   обязательно. Ошибка — НЕМЕДЛЕННЫЙ отказ
  sufficient  достаточно. Успех — остальные не выполняются
  optional    необязательно. Результат игнорируется

Популярные модули:
  pam_unix.so          стандартная Unix-аутентификация (/etc/shadow)
  pam_google_authenticator.so   TOTP (2FA)
  pam_faillock.so      блокировка после N неудачных попыток
  pam_limits.so        ограничения ресурсов (/etc/security/limits.conf)
  pam_nologin.so       блокировать всех если /etc/nologin существует

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m19-04", title="Аудит системы: auditd",
            theory=True,
            brief="""`auditd` — демон аудита ядра Linux. Журналирует события безопасности:
кто, когда, что сделал. Незаменим для compliance (PCI DSS, SOC2, HIPAA).

Установка и запуск:
  apt install auditd audispd-plugins
  systemctl enable --now auditd

Файлы:
  /etc/audit/auditd.conf       конфиг демона (куда писать, размер логов)
  /etc/audit/rules.d/*.rules   правила аудита
  /var/log/audit/audit.log     лог событий

Управление правилами:
  auditctl -l                  # список активных правил
  auditctl -w /etc/passwd -p wa -k passwd-changes
  #  -w путь      наблюдать за файлом/каталогом
  #  -p wa        при записи (w) и изменении атрибутов (a)
  #  -k ключ      тег для поиска в логах

  auditctl -a always,exit -F arch=b64 -S execve -k exec-log
  # логировать каждый запуск программы (execve syscall)

Поиск в логах:
  ausearch -k passwd-changes   # по ключу
  ausearch -ui 1001            # по UID пользователя
  ausearch -x /bin/su          # по имени программы
  ausearch --start today       # с начала сегодняшнего дня

Отчёты:
  aureport                     # общая статистика
  aureport --auth              # отчёт по аутентификациям
  aureport --failed            # только неудачные события

Что стоит отслеживать в продакшне:
  Изменения в /etc/passwd, /etc/shadow, /etc/sudoers
  Запуск программ с SUID
  Подключения к /dev/mem, /dev/kmem
  Все команды пользователей с UID=0

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m19-05", title="fail2ban, ufw и базовое укрепление сервера",
            theory=True,
            brief="""Практический чеклист укрепления нового Linux-сервера.

1. Обновить систему:
  apt update && apt upgrade -y

2. Настроить SSH (см. модуль 13):
  PermitRootLogin no
  PasswordAuthentication no
  AllowUsers deploy alice

3. Firewall (ufw — упрощённый frontend к iptables):
  ufw default deny incoming      # запретить всё входящее
  ufw default allow outgoing     # разрешить всё исходящее
  ufw allow 22/tcp               # SSH
  ufw allow 80/tcp               # HTTP
  ufw allow 443/tcp              # HTTPS
  ufw enable
  ufw status verbose

4. fail2ban — автобан при брутфорсе:
  apt install fail2ban
  # Читает логи, при N неудачах за период — блокирует IP через iptables.
  fail2ban-client status         # список jail-ов
  fail2ban-client status sshd    # статус SSH jail
  fail2ban-client set sshd banip 1.2.3.4   # забанить вручную
  fail2ban-client set sshd unbanip 1.2.3.4 # разбанить

5. Убрать лишние службы:
  systemctl list-units --type=service --state=running
  systemctl disable --now служба   # отключить ненужное

6. Ограничить sudo:
  visudo → конкретным пользователям конкретные команды

7. Автообновления безопасности:
  apt install unattended-upgrades
  dpkg-reconfigure unattended-upgrades

8. Мониторинг целостности файлов:
  apt install aide
  aide --init && aide --check   # проверить изменения системных файлов

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

# ─────────────────────────────────────────────────────────────────────────────
# M20 — Контейнеры изнутри
# ─────────────────────────────────────────────────────────────────────────────

m20 = Module(
    id="m20", title="Контейнеры изнутри: namespaces, cgroups, Docker", level="теория",
    tasks=[
        Task(
            id="m20-01", title="Контейнер ≠ виртуальная машина",
            theory=True,
            brief="""Главное заблуждение: «Docker — это лёгкая VM». Нет.

Виртуальная машина (VM):
  Полная эмуляция железа + отдельное ядро ОС.
  Гипервизор (VMware, KVM, Hyper-V) изолирует VM на уровне железа.
  Гостевая ОС не знает что она в VM (паравиртуализация — знает).
  Старт: минуты. Памяти накладные расходы: сотни МБ (само ядро).
  Полная изоляция: у каждой VM своё ядро.

Контейнер:
  Просто группа процессов с ограниченным «видом» на систему.
  Использует ядро ХОСТОВОЙ системы (своего ядра нет!).
  Изоляция через kernel namespaces + cgroups.
  Старт: миллисекунды (нет загрузки ядра).
  Памяти накладные расходы: мегабайты (только процессы).
  НО: слабее изолированы. Уязвимость в ядре — угроза всем контейнерам.

Что это означает практически:
  В Linux-контейнере нельзя запустить Windows (нет её ядра).
  Docker на Mac/Windows запускает Linux VM, а внутри неё контейнеры.
  «Контейнеры — это процессы» — не метафора, буквально.

  ps aux на хосте покажет процессы из контейнеров.
  Контейнер использует ядро хоста — uname -r одинаков внутри и снаружи.

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m20-02", title="Linux Namespaces: изоляция ресурсов",
            theory=True,
            brief="""Namespace — механизм ядра, который даёт процессу «свой вид» на ресурс.
Процесс в namespace видит только то, что ему показано.

Семь типов namespaces:

PID namespace:
  Процессы видят только свои PID. PID 1 внутри контейнера.
  На хосте это может быть PID 12345.
  unshare --pid --fork bash   # создать новый PID namespace

Network namespace:
  Свои сетевые интерфейсы, таблицы маршрутизации, iptables.
  Внутри контейнера — eth0 с адресом 172.17.0.2.
  На хосте это виртуальный интерфейс vethXXX.
  ip netns list               # список сетевых namespace
  ip netns exec myns ip a     # выполнить команду в namespace

Mount namespace:
  Своё дерево монтирований. Контейнер видит только свою ФС.
  Реализуется через pivot_root() или chroot().

UTS namespace:
  Свой hostname. Контейнер может иметь имя отличное от хоста.

IPC namespace:
  Изолированные inter-process communication (очереди, семафоры).

User namespace:
  Маппинг UID. UID 0 (root) внутри = непривилегированный UID снаружи.
  Позволяет «rootless» контейнеры (без настоящего root на хосте).

Time namespace (новый, Linux 5.6+):
  Разные clock offset для процессов.

Посмотреть namespaces:
  lsns                        # все namespaces на системе
  ls -l /proc/self/ns/        # namespaces текущего процесса
  ls -l /proc/PID/ns/         # namespaces конкретного процесса

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m20-03", title="cgroups: ограничение ресурсов",
            theory=True,
            brief="""cgroups (control groups) — механизм ядра для ограничения и учёта
потребления ресурсов группами процессов.

Что можно ограничить:
  cpu         процент CPU (cpu.shares, cpu.quota)
  memory      максимум RAM и swap
  blkio       скорость дискового I/O
  net_cls     тегирование сетевых пакетов
  pids        максимальное число процессов (защита от fork-bomb)
  devices     разрешённые устройства

cgroups v1 vs v2:
  v1: каждый тип ресурса — отдельная иерархия (сложно).
  v2: единая иерархия для всего (проще, современный стандарт).
  Ubuntu 22.04+, Fedora 31+ используют v2 по умолчанию.

Как это видно в /sys/fs/cgroup/:
  ls /sys/fs/cgroup/                       # cgroups v2
  cat /sys/fs/cgroup/memory.stat           # статистика памяти системы
  ls /sys/fs/cgroup/system.slice/          # cgroup для системных служб

Посмотреть cgroup процесса:
  cat /proc/self/cgroup                    # к какой cgroup принадлежу
  systemd-cgls                            # дерево cgroups через systemd

systemd и cgroups:
  Каждая systemd-служба — отдельная cgroup.
  MemoryMax=512M в unit-файле → ядро ограничит память.
  CPUQuota=50% → максимум 50% одного ядра.

Docker и cgroups:
  docker run --memory=512m --cpus=1 nginx
  # Docker создаёт cgroup и ставит ограничения
  cat /sys/fs/cgroup/docker/<CONTAINER_ID>/memory.max

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m20-04", title="Слоистая ФС: OverlayFS и образы Docker",
            theory=True,
            brief="""Образы Docker состоят из слоёв (layers). Это реализовано через OverlayFS.

Принцип OverlayFS:
  Накладывает несколько директорий («слоёв») в единое дерево.
  Нижние слои — только для чтения (read-only).
  Верхний слой — для записи (read-write).
  При записи файла из нижнего слоя: копируется в верхний, изменяется там
  (copy-on-write). Нижний не трогается.

Как устроен образ Docker:
  Слой 0: ubuntu:22.04 (базовый образ)
  Слой 1: apt install nginx (добавлены файлы nginx)
  Слой 2: COPY ./html /var/www/ (добавлен контент)
  Слой 3: (верхний, R/W) — изменения в работающем контейнере

  Каждый слой — это diff (только изменения). Хранится один раз.
  Несколько контейнеров от одного образа = тысячи контейнеров,
  один набор слоёв. Огромная экономия места.

Посмотреть слои образа:
  docker image inspect nginx | jq '.[].RootFS'
  docker history nginx                          # история слоёв

Посмотреть OverlayFS контейнера:
  docker inspect <id> | jq '.[].GraphDriver'
  # UpperDir — слой записи контейнера
  # LowerDir — слои образа (R/O)
  # MergedDir — смонтированный результат

Что теряется при удалении контейнера:
  Только UpperDir (слой записи). Слои образа — остаются.
  Поэтому volumes нужны для persistence данных.

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m20-05", title="Путь пакета: от docker run до процесса",
            theory=True,
            brief="""Что происходит когда ты пишешь `docker run nginx`?

1. Docker CLI → Docker Daemon (dockerd):
   CLI отправляет REST-запрос daemon-у по unix-сокету /var/run/docker.sock.

2. dockerd → containerd:
   Docker Daemon не управляет контейнерами напрямую.
   Он делегирует containerd — высокоуровневому container runtime.

3. containerd → runc:
   containerd скачивает образ (если нет локально), распаковывает слои.
   Вызывает runc — низкоуровневый OCI-runtime, который непосредственно
   создаёт контейнер.

4. runc:
   Создаёт namespaces: pid, net, mnt, uts, ipc.
   Настраивает cgroups (ограничения ресурсов).
   Монтирует OverlayFS (слои образа + writable layer).
   Вызывает clone() / execve() — запускает процесс PID 1 контейнера.
   Выходит (runc — не daemon, он только запускает).

5. containerd-shim:
   Промежуточный процесс между containerd и контейнером.
   Остаётся жить пока контейнер работает.
   Если containerd упадёт — контейнер продолжает работать.

Стек технологий:
  docker CLI → dockerd → containerd → containerd-shim → runc → контейнер

OCI (Open Container Initiative):
  Стандарт на форматы образов и container runtime.
  Альтернативы: podman (без daemon), nerdctl (для containerd), cri-o.

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

# ─────────────────────────────────────────────────────────────────────────────
# M21 — Сеть изнутри
# ─────────────────────────────────────────────────────────────────────────────

m21 = Module(
    id="m21", title="Сеть изнутри: netfilter, iptables, NAT", level="теория",
    tasks=[
        Task(
            id="m21-01", title="Как пакет проходит через Linux",
            theory=True,
            brief="""Сетевой стек Linux — один из самых сложных компонентов ядра.
Понимание пути пакета помогает диагностировать проблемы и настраивать фильтрацию.

Путь входящего пакета:
  Сетевой адаптер (NIC)
    → Прерывание → Драйвер → Кольцевой буфер (ring buffer)
    → Softirq → Сетевой стек ядра
    → Netfilter hooks (PREROUTING)
    → Таблица маршрутизации
    → Если для нас → Netfilter (INPUT) → Сокет приложения
    → Если транзит → Netfilter (FORWARD) → Исходящий интерфейс

Путь исходящего пакета:
  Приложение → Сокет → Сетевой стек
    → Netfilter (OUTPUT)
    → Таблица маршрутизации (выбор интерфейса)
    → Netfilter (POSTROUTING)
    → Сетевой адаптер → В сеть

Netfilter — фреймворк в ядре:
  5 точек перехвата (hooks): PREROUTING, INPUT, FORWARD, OUTPUT, POSTROUTING.
  iptables, nftables, ipset работают через netfilter.
  conntrack — отслеживание состояния соединений (для stateful firewall и NAT).

Посмотреть состояния соединений:
  cat /proc/net/nf_conntrack | head
  conntrack -L                    # если установлен conntrack-tools

Статистика сетевого стека:
  ss -s                           # статистика сокетов
  ip -s link                      # статистика интерфейсов (ошибки, дропы)
  netstat -s                      # детальная статистика протоколов
  cat /proc/net/dev               # счётчики по интерфейсам

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m21-02", title="iptables: правила фильтрации пакетов",
            theory=True,
            brief="""iptables — классический инструмент управления netfilter.
Работает с таблицами, цепочками и правилами.

Таблицы:
  filter    фильтрация (INPUT, FORWARD, OUTPUT) — по умолчанию
  nat       преобразование адресов (PREROUTING, POSTROUTING, OUTPUT)
  mangle    изменение заголовков пакетов
  raw       обход conntrack

Базовые команды:
  iptables -L -n -v           # список правил (filter) с счётчиками
  iptables -L -n -v -t nat    # правила nat
  iptables -F                 # очистить все правила (осторожно!)
  iptables-save               # сохранить правила в stdout
  iptables-restore < file     # загрузить правила из файла

Структура правила:
  iptables [-t таблица] -A ЦЕПОЧКА [условия] -j ДЕЙСТВИЕ

Добавить правило:
  iptables -A INPUT -p tcp --dport 22 -j ACCEPT   # разрешить SSH
  iptables -A INPUT -s 10.0.0.5 -j DROP           # заблокировать IP
  iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
  iptables -P INPUT DROP                           # политика по умолчанию

Действия (targets):
  ACCEPT    пропустить
  DROP      отбросить (без уведомления)
  REJECT    отклонить (с ICMP ошибкой)
  LOG       логировать
  MASQUERADE  NAT (исходящий адрес = адрес интерфейса)
  DNAT      изменить адрес назначения
  SNAT      изменить адрес источника

Современная альтернатива: nftables (постепенно заменяет iptables):
  nft list ruleset            # показать все правила
  ufw (Ubuntu) и firewalld (RHEL) — высокоуровневые обёртки

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m21-03", title="NAT: как работает маскарадинг",
            theory=True,
            brief="""NAT (Network Address Translation) — замена адресов в пакетах.
Именно благодаря NAT миллиарды устройств используют «серые» адреса.

SNAT (Source NAT) — замена адреса источника:
  Исходящий трафик с частной сети → роутер меняет src IP на свой публичный.
  Ответы приходят на роутер → он помнит соответствие (conntrack) → пробрасывает внутрь.

MASQUERADE — частный случай SNAT:
  Адрес источника берётся динамически с интерфейса (удобно для DHCP).
  iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE

DNAT (Destination NAT) — проброс портов:
  Входящий пакет на порт 8080 хоста → ядро меняет dst на 192.168.1.5:80.
  iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to 192.168.1.5:80
  iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 8080
  # REDIRECT — частный случай DNAT на localhost

Как Docker использует iptables:
  docker run -p 8080:80 nginx   создаёт правило DNAT:
  iptables -t nat -L DOCKER     # увидишь правила Docker

ip_forward — ядро должно быть готово маршрутизировать:
  cat /proc/sys/net/ipv4/ip_forward   # 0 = выключено
  sysctl -w net.ipv4.ip_forward=1     # включить (до перезагрузки)
  echo 'net.ipv4.ip_forward=1' >> /etc/sysctl.conf  # постоянно

Диагностика NAT:
  conntrack -L | grep SNAT     # активные NAT-сессии
  iptables -t nat -L -n -v     # счётчики пакетов через NAT-правила

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m21-04", title="Сетевые namespaces и виртуальные интерфейсы",
            theory=True,
            brief="""Именно так Docker изолирует сеть контейнеров — через network namespaces
и пары veth (virtual ethernet).

veth (virtual ethernet pair):
  Создаётся парой: что войдёт в один конец — выйдет из другого.
  Один конец помещается в namespace контейнера (как eth0).
  Другой конец — на хосте (как vethXXXX), подключён к bridge docker0.

docker0 — Linux bridge:
  Виртуальный коммутатор. Все контейнеры подключаются к нему.
  Контейнеры в одной сети видят друг друга через docker0.
  ip link show docker0    # bridge интерфейс
  brctl show              # таблица bridge (если установлен bridge-utils)

Создать network namespace вручную:
  ip netns add myns                                 # создать namespace
  ip link add veth0 type veth peer name veth1       # создать пару veth
  ip link set veth1 netns myns                      # перенести конец в namespace
  ip netns exec myns ip link set veth1 up           # поднять интерфейс
  ip netns exec myns ip addr add 10.0.0.2/24 dev veth1
  ip link set veth0 up
  ip addr add 10.0.0.1/24 dev veth0
  ip netns exec myns ping 10.0.0.1                  # пинг из namespace на хост

  ip netns list           # список namespace
  ip netns exec myns ss -tulpn   # сокеты внутри namespace

DNS в Docker:
  Контейнеры резолвят имена через встроенный DNS Docker (127.0.0.11).
  Он обращается к /etc/resolv.conf хоста для внешних имён.
  cat /etc/resolv.conf внутри контейнера    # увидишь 127.0.0.11

Нажми Enter, чтобы перейти к следующей странице.""",
        ),
        Task(
            id="m21-05", title="Диагностика сети: полный арсенал",
            theory=True,
            brief="""Системный подход к диагностике сетевых проблем.

Уровень 1 — физика/канал:
  ip link                      # состояние интерфейсов (UP/DOWN, errors)
  ethtool eth0                 # скорость, дуплекс, autoneg
  ip -s link show eth0         # счётчики ошибок

Уровень 2 — IP/маршрутизация:
  ip a                         # адреса
  ip route                     # таблица маршрутизации
  ip route get 8.8.8.8         # какой маршрут для этого адреса
  ping -c4 8.8.8.8             # ICMP до шлюза/интернета
  traceroute 8.8.8.8           # трассировка маршрута
  mtr 8.8.8.8                  # traceroute + ping в реальном времени

Уровень 3 — DNS:
  dig +short google.com        # резолвинг
  dig @8.8.8.8 google.com      # через конкретный DNS
  cat /etc/resolv.conf         # текущий резолвер
  systemd-resolve --status     # если systemd-resolved

Уровень 4 — TCP/порты:
  ss -tulpn                    # что слушает на этой машине
  nc -zv host 443              # доступен ли порт
  curl -v https://example.com  # HTTP с деталями
  telnet host 25               # SMTP handshake

Захват пакетов:
  tcpdump -i eth0 -n           # все пакеты на интерфейсе
  tcpdump -i eth0 port 80      # только HTTP
  tcpdump -i eth0 -w cap.pcap  # записать в файл (открыть в Wireshark)
  tcpdump -i eth0 host 1.2.3.4 # пакеты от/к конкретному IP

Нажми Enter, чтобы продолжить.""",
        ),
    ],
)

MODULES = [m15, m16, m17, m18, m19, m20, m21]
